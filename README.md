# 🏖 Планировщик отпуска

Одностраничное приложение для планирования отпуска. Позволяет собирать информацию о локациях (отели + координаты), добавлять интересные места рядом (рестораны, прогулки, достопримечательности), строить маршруты по дням и открывать их в Яндекс.Картах.

Работает полностью на клиенте — данные хранятся в `localStorage` браузера. Публикуется на GitHub Pages.

## Возможности

- **Локации** — добавление отелей/проживания с координатами
- **Категории мест:**
  - 🍽 Где поесть
  - 🚶 Где погулять
  - 👁 Что посмотреть
- **Интерактивная карта** — отель и все места отображаются маркерами с названиями
- **URL у мест** — возможность указать ссылку на Яндекс.Карты или сайт места
- **Управление местами** — добавление, редактирование и удаление как со страницы локации, так и со страницы маршрута
- **Планировщик маршрутов по дням** — несколько дней, перетаскивание элементов, карта с нумерованными маркерами, заметки на день
- **Визуальный таймлайн** — автоматический расчёт времени начала/окончания для каждого места, настраиваемая длительность (15 мин – 4 ч), время старта дня и цветовая маркировка по категориям
- **Отель в маршруте** — можно добавлять отель (`__hotel__`) в любое место маршрута неограниченное количество раз
- **Добавление мест** — через кнопки «+ Отель» и «+ Место» или клик по доступному месту в правой панели
- **Drag & drop** — сортировка мест внутри категорий и в таймлайне
- **Построение маршрута** — формирование ссылки Яндекс.Карт с точками в порядке маршрута
- **Импорт/экспорт данных** (JSON) — резервное копирование и перенос между устройствами
- **Автомиграция данных** — при загрузке старых данных (версия 2) автоматическое преобразование в актуальный формат
- **Хранение** — стартовые данные из `data/example.json`, изменения сохраняются в `localStorage`

## Структура проекта

```
vacation-planner/
├── index.html              # Точка входа (SPA)
├── config.js               # Конфигурация (API-ключ Яндекс.Карт)
├── css/
│   └── style.css           # Все стили (адаптивная вёрстка, таймлайн, модалки)
├── js/
│   ├── app.js              # Инициализация, роутинг (hash-based), обработка событий
│   ├── ui.js               # Рендер всех страниц, модальных окон, таймлайна
│   ├── store.js            # Слой данных: CRUD + localStorage + миграция v2→v3
│   ├── map.js              # Интеграция Яндекс.Карт API 3.0 (карта, маркеры, геокодер)
│   └── utils.js            # Вспомогательные функции (escHtml, debounce, buildRouteUrl, calcTimeline)
├── data/
│   └── example.json        # Пример данных (Кронштадт, Петергоф) — формат v3
├── CHANGELOG.md            # История версий
└── .gitignore
```

## Технологии

- **Vanilla JS** (ES-модули) — без фреймворков и сборщиков
- **Yandex Maps JavaScript API 3.0** — карта, маркеры, геокодинг
- **HTML5 Drag & Drop API** — сортировка перетаскиванием мест и элементов таймлайна
- **localStorage** — хранение данных
- **CSS Custom Properties** — темизация, цвета категорий и маркеров таймлайна
- **Google Fonts (Nunito)** — современная типографика
- **Автомиграция данных** — бесшовное обновление между версиями формата

## Как запустить

Приложение использует ES-модули (`import`/`export`), поэтому не работает при открытии `index.html` через `file://`. Требуется локальный сервер:

```bash
# Вариант 1: npx serve
npx serve .

# Вариант 2: Python
python -m http.server 8000

# Вариант 3: Node.js
npx http-server .
```

Откройте `http://localhost:3000` (или другой порт) в браузере.

## Получение API-ключа Яндекс.Карт

1. Перейдите в [кабинет разработчика Яндекс.Технологии](https://developer.tech.yandex.ru/services/)
2. Подключите **JavaScript API 3.0**
3. Создайте ключ с ограничением по HTTP Referrer:
   - `localhost:*` — для локальной разработки
   - `*.github.io` — для GitHub Pages
   - `ваш-домен.ru/*` — если используете собственный домен
4. Впишите ключ в `config.js`:

```js
export const APP_CONFIG = {
  ymapsApiKey: 'ВАШ_API_КЛЮЧ'
};
```

### Защита API-ключа

Ключ будет виден в инструментах разработчика браузера — это норма для статического сайта. Основной способ защиты — **ограничение по HTTP Referrer** в кабинете разработчика. Без него ключ сможет использовать кто угодно с любого сайта.

## Публикация на GitHub Pages

1. Создайте репозиторий на GitHub
2. Залейте файлы проекта в корень (ветка `main`)
3. Перейдите: Settings → Pages → Source: **Deploy from a branch**
4. Ветка: `main`, папка: `/ (root)`
5. Через 1–2 минуты сайт будет доступен по адресу `https://ваш-username.github.io/имя-репозитория/`

## Формат данных

При импорте/экспорте используется JSON (актуальная версия — **3**):

```json
{
  "version": 3,
  "locations": [
    {
      "id": "loc-1",
      "name": "Кронштадт",
      "hotelName": "Апартаменты в ЖК Фортеция",
      "coords": { "lat": 59.986092, "lng": 29.796085 },
      "description": "Семейная поездка в Кронштадт на 4 дня"
    }
  ],
  "places": [
    {
      "id": "pl-1",
      "locationId": "loc-1",
      "name": "Никольский Морской собор",
      "category": "see",
      "coords": { "lat": 59.9902, "lng": 29.7795 },
      "description": "Величественный морской собор — символ Кронштадта",
      "order": 0,
      "url": "https://yandex.ru/maps/org/..."
    }
  ],
  "routes": [
    {
      "id": "rt-1",
      "locationId": "loc-1",
      "dayNumber": 1,
      "items": [
        { "placeId": "__hotel__", "duration": 0 },
        { "placeId": "pl-1", "duration": 120 },
        { "placeId": "pl-2", "duration": 45 }
      ],
      "startTime": "10:00",
      "notes": "Заметки на день"
    }
  ],
  "nextIds": { "loc": 3, "pl": 19, "rt": 5 }
}
```

### Изменения между версиями

| Версия | Изменения |
|--------|-----------|
| 1→2    | Добавлено поле `order` у мест |
| 2→3    | `placeIds` → `items[]` (каждый элемент `{ placeId, duration }`), добавлен специальный идентификатор `__hotel__`, добавлено поле `startTime` у маршрутов |
| 3→3.1  | Добавлено необязательное поле `url` у мест (ссылка на Яндекс.Карты или сайт) |

### Автомиграция

При загрузке старых данных (version < 3) приложение автоматически:
- Преобразует `placeIds[]` в `items[]` с `duration: 60` для каждого места
- Добавляет `__hotel__` первым элементом маршрута (если его нет)
- Устанавливает `startTime: "09:00"` для всех маршрутов
